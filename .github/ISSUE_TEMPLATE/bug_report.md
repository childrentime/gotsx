---
name: Bug report
about: Something compiles wrong, renders wrong, or errors where it should not
labels: bug
---

**What happened**

**Minimal `.tsx` that reproduces it** (a page or island; say whether it is `.server.tsx` / `.client.tsx` / shared)

```tsx
```

**Expected** (what the same code does in the browser / what the docs say)

**Environment**
- `gotsx version`:
- `go version`:
- OS:
